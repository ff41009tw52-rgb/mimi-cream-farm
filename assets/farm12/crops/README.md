# Crop stage rules

- 3–5 day crops: seedling -> mature.
- 6+ day crops: seedling -> pre-mature / flower-fruit -> mature when an approved pre-mature asset exists.
- If a stage image is not available, renderer falls back to the existing visual instead of breaking.
- Mature images are treated as approved assets and should not be regenerated/replaced without explicit approval.
