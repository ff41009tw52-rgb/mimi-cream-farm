import{initializeApp}from'https://www.gstatic.com/firebasejs/11.6.1/firebase-app.js';import{getAuth,onAuthStateChanged,signInWithEmailAndPassword,signOut}from'https://www.gstatic.com/firebasejs/11.6.1/firebase-auth.js';import{getFirestore,collection,addDoc,onSnapshot,doc,updateDoc,deleteDoc,getDocs,query,where,serverTimestamp}from'https://www.gstatic.com/firebasejs/11.6.1/firebase-firestore.js';
const cfg={apiKey:'AIzaSyC26_dnG8mHcIepXQMGOX-_-4ulJok9NWQ',authDomain:'bird-650fa.firebaseapp.com',projectId:'bird-650fa',storageBucket:'bird-650fa.firebasestorage.app',messagingSenderId:'657973573508',appId:'1:657973573508:web:288a7518ede39903218fd1'};const A='minan-birds-v2',UID='95kUkYECoMNhPvLqjgMSsgYgpUm1',UH='55d75a7fb38efdd36ed89f802c17cc6cdaf7babfaf2d2d1834e66aa5f4bbff98',EM=atob('ZmY0MTAwOXR3NTJAZ21haWwuY29t');const fb=initializeApp(cfg),auth=getAuth(fb),db=getFirestore(fb),app=document.querySelector('#app');const col=n=>collection(db,'artifacts',A,'public','data',n),d=(n,id)=>doc(db,'artifacts',A,'public','data',n,id);let birds=[],obs=[],admin=false,view='home',selected=null,edit=null;
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const date=v=>v?new Date(v).getTime()||0:0;async function sha(s){let b=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(s));return[...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,'0')).join('')}
function image(file){return new Promise((ok,no)=>{let r=new FileReader;r.onerror=no;r.onload=()=>{let i=new Image;i.onerror=no;i.onload=()=>ok(i);i.src=r.result};r.readAsDataURL(file)})}function canvas(i,max){let q=Math.min(1,max/Math.max(i.width,i.height)),c=document.createElement('canvas');c.width=Math.max(1,Math.round(i.width*q));c.height=Math.max(1,Math.round(i.height*q));c.getContext('2d').drawImage(i,0,0,c.width,c.height);return c}function data(c,q=.72){let x=c.toDataURL('image/webp',q);return x.startsWith('data:image/webp')?x:c.toDataURL('image/jpeg',q)}async function compress(f){let i=await image(f),c=canvas(i,1100),t=canvas(i,360),q=.74,x=data(c,q);for(let k=0;k<10&&x.length>470000;k++){q=Math.max(.46,q-.07);if(q===.46){let z=document.createElement('canvas');z.width=c.width*.86;z.height=c.height*.86;z.getContext('2d').drawImage(c,0,0,z.width,z.height);c=z}x=data(c,q)}let th=data(t,.7);if(x.length+th.length>650000)throw Error('圖片壓縮後仍過大，請換解析度較低的照片。');return{imageData:x,thumbnailData:th}}
async function docs(name,key,val){let s=await getDocs(query(col(name),where(key,'==',val)));return s.docs.map(x=>({id:x.id,...x.data()})).sort((a,b)=>(a.order||0)-(b.order||0))}async function delSet(name,key,val){for(let x of await docs(name,key,val))await deleteDoc(d(name,x.id))}function go(v,x=null){view=v;selected=x||selected;window.scrollTo(0,0);render()}document.querySelector('#brand').onclick=()=>go('home');document.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>go(b.dataset.go));document.querySelector('#loginFoot').onclick=()=>admin?logout():go('login');
onAuthStateChanged(auth,async u=>{admin=u?.uid===UID;if(u&&!admin)await signOut(auth);document.querySelector('#adminNav').hidden=!admin;document.querySelector('#loginFoot').textContent=admin?'登出管理':'管理登入';render()});onSnapshot(col('birds'),s=>{birds=s.docs.map(x=>({id:x.id,...x.data()})).sort((a,b)=>(b.createdAt?.seconds||0)-(a.createdAt?.seconds||0));render()},()=>{app.innerHTML='<div class="emptybox">目前暫時無法取得鳥類資料，請稍後再試。</div>'});onSnapshot(col('observations'),s=>{obs=s.docs.map(x=>({id:x.id,...x.data()})).sort((a,b)=>date(b.observationDate)-date(a.observationDate));render()});
function render(){document.querySelector('#adminNav').hidden=!admin;document.querySelector('#loginFoot').textContent=admin?'登出管理':'管理登入';if(view==='home')home();else if(view==='login')login();else if(view==='admin')adminPage();else if(view==='detail')detail();else if(view==='birdform')birdForm();else if(view==='obsform')obsForm()}
function home(){let first=birds[0];app.innerHTML=`<section class="hero"><div><span class="eyebrow">民安國小自然觀察</span><h1>民安羽跡</h1><h2>校園鳥類觀察站</h2><p>一起記錄，在民安校園裡留下的每一道羽跡。這裡是一本屬於我們的自然觀察圖鑑。</p></div><div class="visual">${first?.coverThumb?`<img src="${first.coverThumb}" alt="${esc(first.name)}">`:`<div class="empty"><div style="font-size:48px">⌁</div><b>等待第一道羽跡</b></div>`}</div></section><section class="section"><div class="sectionhead"><div><h2>校園鳥類</h2><div class="muted">認識民安校園的常客</div></div>${admin?'<button class="btn" id="newBird">＋ 記錄新鳥種</button>':''}</div>${birds.length?`<div class="grid">${birds.map(b=>`<article class="card" data-bird="${b.id}"><div class="cover">${b.coverThumb?`<img src="${b.coverThumb}" alt="${esc(b.name)}">`:'<span>等待照片</span>'}</div><div class="cardbody"><h3>${esc(b.name)}</h3><p>${esc(b.shortDescription||'尚未提供簡介')}</p><div class="link">查看羽跡 →</div></div></article>`).join('')}</div>`:`<div class="emptybox"><div class="emptyicon">⌁</div><h3>第一道羽跡，正等待被發現</h3><p>民安校園的鳥類觀察紀錄將從這裡慢慢累積。</p>${admin?'<button class="btn" id="firstBird">記錄第一種鳥類</button>':''}</div>`}</section>`;document.querySelectorAll('[data-bird]').forEach(e=>e.onclick=()=>{selected=birds.find(b=>b.id===e.dataset.bird);go('detail')});let n=document.querySelector('#newBird')||document.querySelector('#firstBird');if(n)n.onclick=()=>{edit=null;go('birdform')}}
function login(){app.innerHTML=`<div class="login"><div class="box"><div style="text-align:center;font-size:34px">▣</div><h2 style="text-align:center">管理員登入</h2><p class="muted" style="text-align:center">請輸入管理帳號與密碼</p><form id="loginForm"><div class="field"><label>管理帳號</label><input id="u" required autocomplete="username"></div><div class="field"><label>管理密碼</label><input id="p" type="password" required autocomplete="current-password"></div><div id="le"></div><button class="btn" style="width:100%">登入管理中心</button></form><div style="text-align:center;margin-top:18px"><button class="ghost" id="back">返回首頁</button></div></div></div>`;document.querySelector('#back').onclick=()=>go('home');document.querySelector('#loginForm').onsubmit=async e=>{e.preventDefault();let le=document.querySelector('#le');le.innerHTML='';try{if(await sha(document.querySelector('#u').value.trim())!==UH)throw 0;let c=await signInWithEmailAndPassword(auth,EM,document.querySelector('#p').value);if(c.user.uid!==UID){await signOut(auth);throw 0}go('admin')}catch{le.innerHTML='<div class="err">登入失敗，請檢查帳號密碼。</div>'}}}
async function logout(){await signOut(auth);go('home')}
function adminPage(){if(!admin)return go('login');app.innerHTML=`<div class="section"><div class="sectionhead"><div><h2>管理中心</h2><div class="muted">管理鳥類資料與校園觀察紀錄</div></div><div class="actions"><button class="btn btn2" id="newObs">＋ 新增觀察</button><button class="btn" id="newBird">＋ 新增鳥種</button></div></div><div class="box"><h3>已建立的鳥種（${birds.length}）</h3>${birds.length?birds.map(b=>`<div class="adminrow"><img class="thumb" src="${b.coverThumb||''}"><div><b>${esc(b.name)}</b><div class="muted">${esc(b.shortDescription||'')}</div></div><div class="actions"><button class="btn btn2" data-view="${b.id}">查看</button><button class="btn btn2" data-edit="${b.id}">編輯</button><button class="btn danger" data-delbird="${b.id}">刪除</button></div></div>`).join(''):'<p class="muted">尚無鳥類資料。</p>'}</div><div class="box" style="margin-top:22px"><h3>觀察紀錄（${obs.length}）</h3>${obs.length?obs.map(o=>`<div class="adminrow"><img class="thumb" src="${o.firstThumb||''}"><div><b>${esc(birds.find(b=>b.id===o.birdId)?.name||'未知鳥種')}</b><div class="muted">${esc(o.observationDate)} · ${esc(o.location)}</div></div><div class="actions"><button class="btn btn2" data-editobs="${o.id}">編輯</button><button class="btn danger" data-delobs="${o.id}">刪除</button></div></div>`).join(''):'<p class="muted">尚無觀察紀錄。</p>'}</div></div>`;document.querySelector('#newBird').onclick=()=>{edit=null;go('birdform')};document.querySelector('#newObs').onclick=()=>{edit=null;go('obsform')};document.querySelectorAll('[data-view]').forEach(e=>e.onclick=()=>{selected=birds.find(b=>b.id===e.dataset.view);go('detail')});document.querySelectorAll('[data-edit]').forEach(e=>e.onclick=()=>{edit=birds.find(b=>b.id===e.dataset.edit);go('birdform')});document.querySelectorAll('[data-editobs]').forEach(e=>e.onclick=()=>{edit=obs.find(o=>o.id===e.dataset.editobs);go('obsform')});document.querySelectorAll('[data-delbird]').forEach(e=>e.onclick=()=>deleteBird(e.dataset.delbird));document.querySelectorAll('[data-delobs]').forEach(e=>e.onclick=()=>deleteObs(e.dataset.delobs))}
async function deleteBird(id){if(!confirm('確定刪除這個鳥種及其所有觀察紀錄與照片嗎？'))return;for(let o of obs.filter(x=>x.birdId===id))await deleteObs(o.id,true);await delSet('birdPhotos','birdId',id);await deleteDoc(d('birds',id));}async function deleteObs(id,silent=false){if(!silent&&!confirm('確定刪除這筆觀察紀錄嗎？'))return;await delSet('observationPhotos','observationId',id);await deleteDoc(d('observations',id))}
async function detail(){if(!selected)return go('home');app.innerHTML='<div class="loading">正在整理鳥類圖鑑...</div>';let ps=await docs('birdPhotos','birdId',selected.id),oo=obs.filter(x=>x.birdId===selected.id);app.innerHTML=`<div class="detailhero">${ps[0]?`<img src="${ps[0].imageData}" alt="${esc(selected.name)}">`:''}</div><div class="detail"><article class="article"><button class="ghost" id="dh">← 返回校園鳥類</button><h1>${esc(selected.name)}</h1><p class="muted" style="text-align:center">${esc(selected.shortDescription||'')}</p>${ps.length?`<div class="gallery">${ps.map(p=>`<figure><img src="${p.thumbnailData||p.imageData}" data-full="${p.imageData}"><figcaption>${esc(p.caption||'')}</figcaption></figure>`).join('')}</div>`:''}<section><h3>辨識特徵</h3><p>${esc(selected.identification||'尚未提供資料。')}</p></section><section><h3>生活習性</h3><p>${esc(selected.habits||'尚未提供資料。')}</p></section><section><h3>在民安怎麼找到牠？</h3><p>${esc(selected.minanTips||'尚未提供資料。')}</p></section><section><h3>校園觀察紀錄</h3><div id="obsDetail">${oo.length?oo.map(o=>`<div class="obs" data-od="${o.id}"><div class="chips"><span class="chip">${esc(o.observationDate)}</span><span class="chip">${esc(o.location)}</span></div><p>${esc(o.note||'')}</p><div class="odpics"></div></div>`).join(''):'<p class="muted">目前還沒有觀察紀錄。</p>'}</div></section></article></div>`;document.querySelector('#dh').onclick=()=>go('home');document.querySelectorAll('.gallery img').forEach(i=>i.onclick=()=>window.open(i.dataset.full,'_blank'));for(let e of document.querySelectorAll('[data-od]')){let pp=await docs('observationPhotos','observationId',e.dataset.od);e.querySelector('.odpics').innerHTML=pp.length?`<div class="gallery">${pp.map(p=>`<figure><img src="${p.thumbnailData||p.imageData}" data-full="${p.imageData}"><figcaption>${esc(p.caption||'')}</figcaption></figure>`).join('')}</div>`:''}document.querySelectorAll('.odpics img').forEach(i=>i.onclick=()=>window.open(i.dataset.full,'_blank'))}
function filePicker(max){return`<div class="field"><label>照片（最多 ${max} 張）</label><div class="picker"><input id="files" type="file" accept="image/*" multiple><div id="previews" class="previews"></div></div></div>`}function bindPicker(max){let inp=document.querySelector('#files'),box=document.querySelector('#previews');let items=[];inp.onchange=()=>{items=[...inp.files].filter(f=>f.type.startsWith('image/')).slice(0,max).map((f,i)=>({f,url:URL.createObjectURL(f),caption:''}));box.innerHTML=items.map((x,i)=>`<div class="preview"><img src="${x.url}"><input data-cap="${i}" placeholder="照片說明（選填）"></div>`).join('');box.querySelectorAll('[data-cap]').forEach(x=>x.oninput=()=>items[+x.dataset.cap].caption=x.value)};return()=>items}
function birdFields(v={}){return`<div class="field"><label>鳥類名稱 *</label><input id="name" required value="${esc(v.name||'')}"></div><div class="field"><label>首頁簡短介紹 *</label><textarea id="short" required rows="2">${esc(v.shortDescription||'')}</textarea></div><div class="field"><label>辨識特徵</label><textarea id="ident" rows="4">${esc(v.identification||'')}</textarea></div><div class="field"><label>生活習性</label><textarea id="habits" rows="4">${esc(v.habits||'')}</textarea></div><div class="field"><label>在民安怎麼找到牠？</label><textarea id="tips" rows="3">${esc(v.minanTips||'')}</textarea></div>`}
async function birdForm(){if(!admin)return go('login');app.innerHTML=`<div class="panel"><button class="ghost" id="back">← 返回管理中心</button><div class="box"><h2>${edit?'編輯鳥種':'新增鳥種'}</h2><form id="bf">${birdFields(edit||{})}${filePicker(8)}<button class="btn" style="width:100%">儲存鳥種資料</button></form></div></div>`;document.querySelector('#back').onclick=()=>go('admin');let getFiles=bindPicker(8);document.querySelector('#bf').onsubmit=async e=>{e.preventDefault();let data={name:name.value.trim(),shortDescription:short.value.trim(),identification:ident.value.trim(),habits:habits.value.trim(),minanTips:tips.value.trim(),updatedAt:serverTimestamp()},id=edit?.id;if(id)await updateDoc(d('birds',id),data);else{let r=await addDoc(col('birds'),{...data,createdAt:serverTimestamp(),coverThumb:''});id=r.id}let items=getFiles(),first='';for(let i=0;i<items.length;i++){let z=await compress(items[i].f);if(!first)first=z.thumbnailData;await addDoc(col('birdPhotos'),{birdId:id,...z,caption:items[i].caption,order:Date.now()+i,createdAt:serverTimestamp()})}if(first)await updateDoc(d('birds',id),{coverThumb:first});edit=null;go('admin')}}
function obsFields(v={}){let loc=v.location||'前操場';return`<div class="twocol"><div class="field"><label>鳥種 *</label><select id="bird" required><option value="">-- 請選擇 --</option>${birds.map(b=>`<option value="${b.id}" ${b.id===v.birdId?'selected':''}>${esc(b.name)}</option>`).join('')}</select></div><div class="field"><label>觀察日期 *</label><input id="odate" type="date" required value="${esc(v.observationDate||new Date().toISOString().slice(0,10))}"></div></div><div class="field"><label>觀察地點 *</label><input id="loc" required value="${esc(loc)}"></div><div class="field"><label>觀察補充</label><textarea id="note" rows="4">${esc(v.note||'')}</textarea></div>`}
async function obsForm(){if(!admin)return go('login');if(!birds.length){alert('請先新增至少一種鳥類。');return go('admin')}app.innerHTML=`<div class="panel"><button class="ghost" id="back">← 返回管理中心</button><div class="box"><h2>${edit?'編輯觀察紀錄':'新增觀察紀錄'}</h2><form id="of">${obsFields(edit||{})}${filePicker(12)}<button class="btn" style="width:100%">儲存觀察紀錄</button></form></div></div>`;document.querySelector('#back').onclick=()=>go('admin');let getFiles=bindPicker(12);document.querySelector('#of').onsubmit=async e=>{e.preventDefault();let data={birdId:bird.value,observationDate:odate.value,location:loc.value.trim(),note:note.value.trim(),updatedAt:serverTimestamp()},id=edit?.id;if(id)await updateDoc(d('observations',id),data);else{let r=await addDoc(col('observations'),{...data,createdAt:serverTimestamp(),firstThumb:'',photoCount:0});id=r.id}let existing=await docs('observationPhotos','observationId',id),items=getFiles(),first=existing[0]?.thumbnailData||'',count=existing.length;for(let i=0;i<items.length;i++){let z=await compress(items[i].f);if(!first)first=z.thumbnailData;await addDoc(col('observationPhotos'),{observationId:id,birdId:data.birdId,...z,caption:items[i].caption,order:Date.now()+i,createdAt:serverTimestamp()});count++}await updateDoc(d('observations',id),{firstThumb:first,photoCount:count});edit=null;go('admin')}}
render();