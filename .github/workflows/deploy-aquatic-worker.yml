name: Retired aquatic Cloudflare deployment

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Cloudflare deployment is retired
        run: echo "Aquatic observation now uses Google Apps Script, Google Sheets, and Google Drive. No R2 action is performed."
